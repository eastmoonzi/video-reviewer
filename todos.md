硬编码 apikey 无需修改；
评分系统无需修改；
localStotage 暂时无需变动；
任务搜索功能可以添加，优先级往后放一放；
视频播放器的倍速和时间轴功能优化优先级放到最低；评分面板的两套实现具体是什么？；
自动保存状态提示要加上

```
注意： 官方极其不推荐在生产环境（正式上线）中使用 <script src="https://cdn.tailwindcss.com"></script>。

原因： 这个脚本是实时编译运行的，不仅体积大、性能差，而且容易受 CDN 状态影响导致样式崩溃。

官方方案： 使用 Tailwind CLI 或 PostCSS 进行构建，生成压缩后的纯 CSS 文件。
```